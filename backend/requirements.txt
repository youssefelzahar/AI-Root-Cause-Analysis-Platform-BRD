# Pinned so two builds a week apart produce the same image. Regenerate with
# `pip freeze` after any intentional upgrade.

# --- web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20        # required by FastAPI for multipart uploads
httpx==0.28.1

# --- database ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- analytics ---
duckdb==1.1.3                   # out-of-core CSV/Parquet profiling
pyarrow==18.1.0                 # bounded-memory Parquet writes
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
openpyxl==3.1.5                 # streaming xlsx reads

# --- sql server ---
pymssql==2.3.2                  # manylinux wheels, no system ODBC driver needed
sqlglot==26.0.1                 # AST-based read-only statement guard

# --- security / logging ---
cryptography==44.0.0            # Fernet encryption for stored SQL credentials
python-json-logger==3.2.1
